export const getUsersPosts = state => {
    return state.profilePage.userPosts;
}

export const getProfile = state => {
    return state.profilePage.profile;
}

export const getStatus = state => {
    return state.profilePage.status;
}